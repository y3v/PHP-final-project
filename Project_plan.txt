Chat system :
Sending direct messages to a user asynchronously.

	FrontEnd
	- html/js interface.
	- Send button fires javascript function that sends AJAX post request to server.
		response data (if query successfully executed) would be
		-> conversation object
	- Processes messages with an ajax request.
	- If recipient, messages are on the left
	- If user, messages are on the right & colored
	
	PHP
		Classes : 
		
	
	SQL
	Tables : 
		user
		-> id
		-> isLoggedIn
		-> isTyping
		
		conversation
		-> id
		-> sender (id-user1)
		-> recipient (id-user2)
		-> timeStamp
		-> message
		(optional)
		-> viewed_by_recipient

Timeline : 